<Window x:Class="WebDavTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebDavTools"
        mc:Ignorable="d"
        Title="WebDAV Tools" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <ComboBox SelectedIndex="0">
                <ComboBoxItem>Serveur Yanos</ComboBoxItem>
            </ComboBox>

            <TextBox x:Name="tbPath" Grid.Column="1" Text="/" />
        </Grid>

        <Grid Grid.Row="1">
            <Button Click="btnTest_Click">Test</Button>
        </Grid>

        <TextBox x:Name="tbLog" Grid.Row="2" AcceptsReturn="True" FontFamily="Consolas" FontSize="11pt">
            
        </TextBox>
        
    </Grid>
</Window>
